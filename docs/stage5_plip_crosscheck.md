# Stage 5 PLIP Cross-Check

PLIP is optional in Stage 5 and is used only as an independent QA layer. PLIP outputs are stored separately and do not overwrite ProLIF or fallback interaction fingerprints.
