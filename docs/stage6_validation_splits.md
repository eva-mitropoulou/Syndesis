# Stage 6 Validation Splits

The primary split is scaffold holdout when scaffold identifiers can be produced from available ligands. Ligand holdout and receptor holdout are also generated. Random pose-level splitting is not used as the primary split.

Splits are assigned at group level so all poses for a docking task remain together.
