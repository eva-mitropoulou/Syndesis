# Stage 5 Source Registry

The Stage 5 source registry is stored in `data/references/stage5_sources.yaml`. Each entry records the supported claim, citation metadata, DOI or URL, use in the workflow, and confidence level.
