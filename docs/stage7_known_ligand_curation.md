# Stage 7 Known Ligand Curation

Known EGFR ligands are retrieved from ChEMBL using the discovered P00533 single-protein target and from BindingDB using the current REST UniProt endpoint. Stage 1 native ligands are imported as structural references. Endpoint types remain separate.
