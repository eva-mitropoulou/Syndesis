# Stage 10 Benchmark Protocol

Stage 10 evaluates Stage 9 analog optimization outputs under paired, budget-controlled, multi-metric conditions. It consumes existing tables only by default.

Stage 10 does not run docking, GNINA, ProLIF, pose-confidence scoring, agent generation, or MD. MD-containing ablations are marked `pending_stage11` until Stage 11 outputs exist.
