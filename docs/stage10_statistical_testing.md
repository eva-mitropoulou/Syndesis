# Stage 10 Statistical Testing

Stage 10 uses seed-level paired accepted rates. It computes bootstrap confidence intervals over seed-level differences, paired permutation p-values, effect-size proxies, and Benjamini-Hochberg corrected p-values.

When seed count is small, outputs are explicitly labeled exploratory and should not be used for strong significance claims.
