# Stage 9 Source Registry

The YAML registry at `data/references/stage9_sources.yaml` traces literature and tool claims for tool-grounded molecular agents, REINVENT4, BRICS, matched molecular pairs, local LLM choices, and medchem filters.

Every registry row includes a source id, supported claim, title, year, source type, DOI or URL, usage, notes, and confidence level.
