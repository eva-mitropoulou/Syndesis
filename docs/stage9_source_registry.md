# Stage 9 Source Registry

The YAML registry at `data/references/stage9_sources.yaml` traces literature and tool claims for RDKit transformations, BRICS, matched molecular pairs, and medicinal-chemistry filters.

Every registry row includes a source id, supported claim, title, year, source type, DOI or URL, usage, notes, and confidence level.
